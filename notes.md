things to implement:

- .size()
	- For original size, e.g. pixmap size
- .resize(dimension)
	- resizes widget to the dimension
- setPixmap( image )
	- Set image of widget
- mapFromGlobal( point )
	- Converts global point to local point
	- Should have by default
